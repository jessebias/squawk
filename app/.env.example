# Privy (dashboard.privy.io → your app → App settings)
# App client must be a mobile client with Android package
# com.squawk.app and URL scheme "squawk".
# Leave empty to run without Privy (local burner + MWA only).
EXPO_PUBLIC_PRIVY_APP_ID=
EXPO_PUBLIC_PRIVY_CLIENT_ID=
